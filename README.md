# war.re

Personal site at [war.re](https://war.re) and resume at [ryan.war.re](https://ryan.war.re).

## Layout

- `main/` — Next.js app for war.re
- `subdomains/ryan/` — Next.js app for ryan.war.re (resume)

Each app is independent (own `package.json`, `yarn.lock`, build). See each
app's README for dev commands.
